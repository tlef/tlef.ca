<% layout('layouts/base') %>

<div class="site-container">
  <div class="home-grid">
    <main class="main-content">
      <section>
        <h2 class="main-section-header">Latest Articles</h2>
        
					<div class="featured-posts">
						<% if (it.posts && it.posts.length) { %>
							<% it.posts.forEach(function(post) { %>
								<article class="featured-post">
									<h3 class="featured-post-title">
									<a href="/posts/<%= post.slug %>" data-spa-link><%= post.title || post.slug %></a>
									</h3>
									<% if (post.summary) { %>
									<div class="featured-post-excerpt"><%= post.summary %></div>
									<% } else { %>
									<div class="featured-post-excerpt">An insightful article about software development, web technologies, and building great applications. Click to read the full post and learn something new.</div>
									<% } %>
									<a href="/posts/<%= post.slug %>" data-spa-link class="read-more-link">Read more</a>
								</article>
							<% }) %>
						<% } else { %>
							<div class="featured-post-excerpt">No articles found.</div>
						<% } %>
						</div>
						
						<p style="text-align: left; margin-top: 2rem;">
						<a href="/posts" data-spa-link style="font-weight: 600; color: var(--accent-color);">View all articles →</a>
						</p>
          
      </section>
    </main>

    <!-- Sidebar column (1/3) -->
    <aside class="sidebar">
      <div class="sidebar-section">
        <h3 class="sidebar-title">Categories</h3>
        <ul class="sidebar-list">
				<% if (it.categories && it.categories.length) { %>
					<% it.categories.forEach(function(category) { %>
						<li>
							<a href="/posts?tag=<%= category.tag %>" data-spa-link>
								<%= category.title %> <span class="category-count">(<%= category.count %>)</span>
							</a>
						</li>
					<% }) %>
				<% } %>
        </ul>
      </div>

      <div class="sidebar-section">
        <h3 class="sidebar-title">Popular Posts</h3>
        <ul class="sidebar-list">
				<% if (it.popularPosts && it.popularPosts.length) { %>
					<% it.popularPosts.forEach(function(post) { %>
						<li>
							<a href="/posts/<%= post.slug %>" data-spa-link>
								<%= post.title %>
							</a>
						</li>
					<% }) %>
				<% } %>
        </ul>
      </div>

      <div class="sidebar-section">
        <h3 class="sidebar-title">About</h3>
        <p style="color: var(--muted-text); line-height: 1.6; font-size: 0.925rem;">
          Hi, I'm Tim. I write about software development, web technologies, and building great user experiences. 
          <a href="/about" data-spa-link style="color: var(--accent-color);">Learn more →</a>
        </p>
      </div>
    </aside>
  </div>
</div>
